/** Shop contact — swap this number when the client confirms WhatsApp. Digits only, with country code. */
export const WHATSAPP_NUMBER =
  process.env.NEXT_PUBLIC_WHATSAPP_NUMBER?.replace(/\D/g, "") ?? "";

export const INSTAGRAM_URL = "https://www.instagram.com/the_home_shopee";
export const INSTAGRAM_HANDLE = "@the_home_shopee";
